package se.liu.ida.joaos226.tddd78.project.game_logic;

/**
 * this enum class specifies what direction an image is pointing at and that info is then used to determine what image to load
 * and display on the screen so it faces the correct way.
 */
public enum Direction
{
    LEFT, RIGHT, OMNI
}
